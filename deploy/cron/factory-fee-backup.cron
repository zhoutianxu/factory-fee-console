# Run `crontab deploy/cron/factory-fee-backup.cron` after changing the project path if needed.
# Default: backup runtime data every day at 02:30.
30 2 * * * cd /opt/factory-fee-console && /bin/bash scripts/aliyun_backup.sh >> runtime/backups/backup.log 2>&1
